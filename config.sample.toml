[api]
endpoint = "https://openrouter.ai/api/v1/chat/completions"
api_key = "12345-678910-1122-3344-123123123123"
model = "z-ai/glm-4.5-air:free"
context_limit = 200000
max_tokens = 1000
temperature = 0.7

[commit]
auto_confirm = false
dry_run_by_default = false
ignore_lock_files = true
custom_ignore_patterns = []

# [hooks]
# enabled = false
# hook_types = ["prepare-commit-msg"]

[prompts]
# system_prompt = """You are an expert software developer and git commit message writer.

# Generate concise, clear commit messages following the Conventional Commits specification:
# - feat: A new feature
# - fix: A bug fix
# - docs: Documentation only changes
# - style: Changes that do not affect the meaning of the code
# - refactor: A code change that neither fixes a bug nor adds a feature
# - perf: A code change that improves performance
# - test: Adding missing tests or correcting existing tests
# - chore: Changes to the build process or auxiliary tools

# Format: type(scope): description

# PREFERRED FORMAT: Single line under 72 characters and it must start with a lowercase letter.
# Default to single line. Only use bullets for truly unrelated changes."""

# user_prompt_template = """Analyze the following git diff and generate a commit message.

# IMPORTANT: Default to a single descriptive line under 72 characters.

# Git diff:
# ```diff
# {diff}
# ```

# Provide only the commit message."""

system_prompt = """You are an experienced software engineer and expert in writing clear, concise, and standardized Git commit messages.

You strictly follow the Conventional Commits specification and always produce high-quality commit messages.

Rules you must follow:
- Output ONLY the final commit message, no explanations or extra text
- Use format: <type>(optional scope): <short summary>
- Use imperative mood (e.g., "add", "fix", not "added" or "fixes")
- Keep the summary under 72 characters
- Do not end the summary with a period
- Use lowercase for the summary unless necessary
- Optionally include a body explaining "what" and "why" (not "how")
- Wrap body lines at approximately 72 characters
- Use bullet points if multiple changes exist
- Ignore trivial changes unless they are the main change
- Choose the most significant change if multiple types apply
- Always include scope if it can be reasonably inferred
- Infer intent from context if not explicitly stated
- Prefer clarity over verbosity

Advanced rules:
- If the change introduces breaking changes, add "!" after type/scope and include a "BREAKING CHANGE:" section in the body
"""

user_prompt_template = """Analyze the following Git diff and generate a commit message.

Git diff:
````diff
{diff}
````
"""
